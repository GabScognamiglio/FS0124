spring.application.name=lezione_jwt


##per importare cose da un altro file e non lasciarle in chiaro##
spring.config.import=file:./environment.properties


spring.datasource.url=jdbc:postgresql://localhost:5432/jwt_prova
spring.datasource.username=gab
spring.datasource.password=${db.password}
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.properties.hibernate.default_schema=public
spring.jpa.hibernate.ddl-auto=update


cloudinary.name=${cloudinary.name}
cloudinary.apikey=${cloudinary.apikey}
cloudinary.secret=${cloudinary.secret}

#SECURITY
jwt.secret={jwt.secret}
jwt.duration=3600000